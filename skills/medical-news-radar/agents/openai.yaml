interface:
  display_name: "Medical News Radar"
  short_description: "Review and maintain medical news sources"
  default_prompt: "Use $medical-news-radar to assess this medical source, probe it safely, and route it without exposing secrets or weakening the static GitHub architecture."
policy:
  allow_implicit_invocation: true
